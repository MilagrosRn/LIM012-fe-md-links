/*
CALLBACK  it("deberia retornar una ruta absoluta a partir de una ruta relativa",
  // dada una relativa y sale una absoluta
  // dada una ruta absoluta en la funcion devuelve la misma ruta
  // expect(convertPathRelative(pathRelative).toBe(pathAbsolute)), expect(convertPathRelative(pathAbsolute).toBe(pathAbsolute))
  
// PROMESA  it("deberia comprobar si es directorio o archivo", () =>{
PROMESA  it("debería retornar un arreglo de las rutas de todos los archivos al ingresar una ruta absoluta)
 //dada la ruta de un dir ,true salen las rutas
   //dada la ruta de un archivo,false en la funct duelve la misma ruta
   //expect(readDirectory(pathAbsoluteFile)).toEqual(arrPathInFile)

PROMESA  it(" debería retornar un arreglo de solo rutas de archivos markdown al ingresar un arreglo de rutas de archivos")
     //expect(entran rutas encontradas y sales rutas solo con mds)
        //dado un array con rutas de archivos donde ninguno se md sale *no se encontro archivos md*


test('the data will be peanut butter', done => {
  function callback(data) {
    expect(data).toBe('peanut butter');
    done();
  }

  fetchData(callback);
});

test('doAsync calls both callbacks', () => {
    expect.assertions(1);
    function callback1(data) {
      expect(data).toBeTruthy();
    }
    doAsync(callback1);
  });

  PROMESA it('deberia retornar una promesa que resuelva a un arreglo de objetos, donde cada objeto representa un link')
  //expect(entra ruta de un archivo md con links y sale un array de objetos href,link y file)
  //expect(entra ruta deun archivo md sin links  .tobe array vacio)
  FUNCION it('deberia devolver un objeto con propiedad text no mayor a 50 caracteres')
  
.toHaveLength(50)

cutCaracteresText
'deberia devolver un objeto con propiedad text no mayor a 50 caracteres'
//expect(entra un array de objetos y sale un array de objetos con propiedad text no mayor a 50 caracteres)
it("deberia comprobar si la ruta existe", done => {
    let callbackInterno =(datos)=>{
      try{
        expect(datos).tobe(true)
        done()
      }catch (error) {
        done(error);
      }
    existPath(callbackInterno)
    }
      
  });

// CLI
'Debería retornar el arreglo de objetos ingresado con las propiedades '
toEqual
linksUnicos
it('deberia dar la cantidad de links unicos',()=>{

})
// entra un ojeto con href,text, file y sale cantidad de href no repetidos
toEqual
 statsValidateLinks 
  'debería retornar las estadisticas y validaciones del archivo'
// entra un ((array con obj href,link y file,status y statusText),{validate: true,stats:true}) y sale {cantidad:3,unicos:2,broken: 1}
 
});
ERRORES EN UNA PROMESA
it('tests error with promises', () => {
    expect.assertions(1);
    return user.getUserName(2).catch(e =>
      expect(e).toEqual({
        error: 'User with 2 not found.',
      }),
    );
  });
*/